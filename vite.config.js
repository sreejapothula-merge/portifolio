import { defineConfig } from "vite";

// IMPORTANT: set this to your repo name for GitHub Pages project sites,
// e.g. if your repo is github.com/you/portfolio, base should be "/portfolio/".
// If you deploy to a custom domain or a user/org page (you.github.io), use "/".
export default defineConfig({
  base: "/portifolio/",
});
